!!!
%html
	%head
		%title= name
		= stylesheet_link_tag "application", media: "all"
		= javascript_include_tag "application"
		= csrf_meta_tags
	%body{ class: controller.controller_name }
		#banner.rounded
			= link_to image_tag("sign.jpg"), root_path
			.banner_title Homicide Life on the Street
			.navigation_links
				%br
				= link_to "Admin", admin_root_path
		#main_content
			#columns.rounded
				#side
					= yield :sidebar
				#main
					- if flash.notice
						#notice= flash.notice
					- if flash.alert
						#alert= flash.alert
					%div{"data-pjax-container" => ""}
						= yield
					= debug(params) if Rails.env.development?